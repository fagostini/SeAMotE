#define MAX_ID 100
#define MAX_SEQ 15000
#define MIN_MOT 2
#define MAX_MOT 20
#define NUM_BOOT 100000	/* Gives the precision of the p-value */
#define NUM_THREADS	2	/* This number MUST NOT change!!! */
#define NO_NEGA 1		/* Is the factor size that will be use to generate the negative, if not given. 1 means that positive and negative set sizes will be equal */  
#define NUM_RAND 10		/* Raise this number to generate X sets of randomized sequences */
#define NUM_SHUFFLE 10	/* Raise this number to generate X sets of shuffled sequences */
#define P_TEST 1		/* Raise this number to increase the number of shuffle and random permutations (NOT IN USE AT THE MOMENT) */
